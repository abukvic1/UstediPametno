@using UstediPametno.Models
@model UstediPametno.ViewModels.TransakcijeIndexViewModel
@{
    ViewData["Title"] = "Transakcije";
}

@if (TempData["Greska"] != null)
{
    <div class="alert alert-danger">
        @TempData["Greska"]
    </div>
}

<div class="page-title">
    <h1>Transakcije</h1>
    <p>Pregled svih prihoda, troškova i uplata štednje.</p>
</div>
<div class="row g-3 mb-4">

    <div class="col-md-6 col-xl-3">
        <div class="card shadow-sm h-100">
            <div class="card-body">
                <div class="text-muted mb-2">
                    <i class="bi bi-wallet2 me-1"></i>
                    Raspoloživo
                </div>

                <h3 class="mb-0">
                    @Model.Raspolozivo.ToString("N2") KM
                </h3>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-xl-3">
        <div class="card shadow-sm h-100">
            <div class="card-body">
                <div class="text-muted mb-2">
                    <i class="bi bi-arrow-up-circle me-1"></i>
                    Prihodi
                </div>

                <h3 class="mb-0 text-success">
                    @Model.UkupniPrihodi.ToString("N2") KM
                </h3>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-xl-3">
        <div class="card shadow-sm h-100">
            <div class="card-body">
                <div class="text-muted mb-2">
                    <i class="bi bi-arrow-down-circle me-1"></i>
                    Potrošeno
                </div>

                <h3 class="mb-0 text-danger">
                    @Model.UkupniRashodi.ToString("N2") KM
                </h3>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-xl-3">
        <div class="card shadow-sm h-100">
            <div class="card-body">
                <div class="text-muted mb-2">
                    <i class="bi bi-piggy-bank me-1"></i>
                    Ušteđeno
                </div>

                <h3 class="mb-0 text-primary">
                    @Model.UkupnoUstedjeno.ToString("N2") KM
                </h3>
            </div>
        </div>
    </div>

</div>
<p>
    <a asp-action="Create"
       class="btn btn-primary">
        <i class="bi bi-plus-lg me-1"></i>
        Dodaj novu transakciju
    </a>
</p>

@if (!Model.Transakcije.Any())
{
    <div class="alert alert-info">
        Još uvijek nemaš evidentiranih transakcija.
    </div>
}
else
{
    <div class="table-container">
        <div class="table-responsive">

            <table class="table align-middle">

                <thead>
                    <tr>
                        <th>Datum</th>
                        <th>Opis</th>
                        <th>Vrsta</th>
                        <th>Iznos</th>
                        <th>Mjesečni plan</th>
                        <th>Cilj štednje</th>
                        <th>Akcije</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var transakcija in Model.Transakcije)
                    {
                        string vrstaTekst =
                            transakcija.Vrsta switch
                            {
                                VrstaTransakcije.Prihod =>
                                    "Prihod",

                                VrstaTransakcije.FiksniTrosak =>
                                    "Fiksni trošak",

                                VrstaTransakcije.DnevnaPotrosnja =>
                                    "Dnevna potrošnja",

                                VrstaTransakcije.UplataStednje =>
                                    "Uplata štednje",

                                _ =>
                                    transakcija.Vrsta.ToString()
                            };

                        bool jestePrihod =
                            transakcija.Vrsta ==
                            VrstaTransakcije.Prihod;

                        bool jesteUplataStednje =
                            transakcija.Vrsta ==
                            VrstaTransakcije.UplataStednje;

                        string bojaIznosa =
                            jestePrihod
                                ? "amount-income"
                                : jesteUplataStednje
                                    ? "amount-saving"
                                    : "amount-expense";

                        <tr>

                            <td>
                                @transakcija.Datum.ToString("dd.MM.yyyy.")
                            </td>

                            <td>
                                <strong>
                                    @transakcija.Opis
                                </strong>
                            </td>

                            <td>
                                @vrstaTekst
                            </td>

                            <td class="@bojaIznosa">
                                @if (jestePrihod)
                                {
                                    <span>+</span>
                                }
                                else
                                {
                                    <span>-</span>
                                }

                                @transakcija.Iznos.ToString("N2") KM
                            </td>

                            <td>
                                @if (transakcija.NazivPlana == "Nema plana")
                                {
                                    <span class="text-muted">
                                        Nema plana
                                    </span>
                                }
                                else
                                {
                                    <span>
                                        @transakcija.NazivPlana
                                    </span>
                                }
                            </td>

                            <td>
                                @if (transakcija.NazivCilja == "-")
                                {
                                    <span class="text-muted">
                                        -
                                    </span>
                                }
                                else
                                {
                                    <span class="badge-saving">
                                        @transakcija.NazivCilja
                                    </span>
                                }
                            </td>

                            <td class="text-nowrap">

                                <a asp-action="Edit"
                                   asp-route-id="@transakcija.Id"
                                   class="btn btn-warning btn-sm">

                                    <i class="bi bi-pencil me-1"></i>
                                    Izmijeni
                                </a>

                                <a asp-action="Delete"
                                   asp-route-id="@transakcija.Id"
                                   class="btn btn-danger btn-sm">

                                    <i class="bi bi-trash me-1"></i>
                                    Obriši
                                </a>

                            </td>

                        </tr>
                    }

                </tbody>

            </table>

        </div>
    </div>
}